# MakeupOcean Mobile App Environment Configuration

# API Configuration
# Development: Use local IP address (find with: ipconfig on Windows, ifconfig on Mac/Linux)
API_BASE_URL=http://192.168.1.xxx:3000/api/v1

# Production: Your deployed BFF server URL
# API_BASE_URL=https://your-bff-server.onrender.com/api/v1

# API Key (must match the key configured in BFF server)
API_KEY=your-api-key-here

# Google OAuth Client IDs
# Create these in Google Cloud Console -> APIs & Services -> Credentials
# Web Client ID (required for expo-auth-session)
GOOGLE_CLIENT_ID_WEB=your-web-client-id.apps.googleusercontent.com

# iOS Client ID (optional, for native iOS builds)
GOOGLE_CLIENT_ID_IOS=your-ios-client-id.apps.googleusercontent.com

# Android Client ID (optional, for native Android builds)
GOOGLE_CLIENT_ID_ANDROID=your-android-client-id.apps.googleusercontent.com

# Razorpay (for reference - key is fetched from backend)
# RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx

# App Configuration
APP_NAME=MakeupOcean
APP_SCHEME=muoapp
